# Echo 下一轮修复计划

更新时间：2026-05-23

## 1. 设置页里的“服务器地址”

当前“服务器地址”用于指定前端请求后端 API 的地址，例如 ECS 的 `http://8.140.194.214:9090` 或域名后的 API 地址。

它不是数据库密码，也不是服务器登录密码。IP 地址本身不是机密，别人知道 IP 不代表能登录服务器。

但这里有几个风险要处理：

- 当前如果使用 `http://`，登录 token 和接口数据在网络层不是端到端加密，公共 Wi-Fi 下不够安全。
- 普通用户不应该看到或修改“服务器地址”，否则可能误填导致无法登录，或者被诱导改到假服务器。
- 等正式域名下来后，应固定为 HTTPS API 地址，普通设置页隐藏这个输入，只保留开发调试入口。

建议：

- 开发者模式才显示“服务器地址”。
- 普通用户设置页不显示该项。
- 正式环境使用 HTTPS 域名，不再让用户手动输入 ECS IP。
- 前端只允许同源或白名单 API 地址，避免被改到恶意后端。

## 2. “与 Shoot 的星域”页面问题

截图页面整体方向是对的，但现在数据空得太多，体验还不完整。

### 需要修复的问题

- “资料设置”按钮点击没有反应。
- 资料设置应该进入好友设置页，也就是以前聊天页点击好友名字进入的页面。
- 聊天页点击好友名字进入设置要关闭。
- 好友设置页的唯一入口改成：聊天页左滑进入“关系星域” -> 点击“资料设置”。
- 从好友设置页返回后，应回到“与 XXX 的星域”，不是回聊天页或会话列表。

### 关系数据区调整

当前四个块：

- 所属星域
- 认识时间
- 最近互动
- 共同图片

需要改成更有用的四个块：

- 所属星域
- 认识时间
- 回声值
- 最近连接

说明：

- “共同图片”不要了。
- “共同图片”位置改成“回声值”，但不要叫“亲密度”，避免像 QQ。
- 回声值由聊天、点赞动态、评论动态、互相访问、语音/通话等互动累积。
- 后续需要有一个独立页面查看“我的好友回声值排行”。

## 3. 所属星域逻辑

一个好友可以属于多个星域，例如：

- 家人星域
- 至亲星域
- 同学星域

但“关系星域”页只展示一个主星域。

规则建议：

- 每个好友允许设置一个"所属星域"作为"关系星域"页面"所属星域"展示的“星域”。
- 如果未设置，默认取该好友所属星域里的第一个。
- 如果没有任何星域，显示“暂无”。
- 点击“展示星域”卡片，打开自定义弹窗选择一个星域。
- 弹窗不能用浏览器原生弹窗，要用项目自己的 Modal 样式。

后端需要保存：

- `Friend.displayGroupId` 或单独的关系设置表字段。

## 4. 认识时间逻辑

现在“认识时间”显示暂无，不够好。

需要改成：

- 默认显示软件内互相加好友成功的日期，格式：`2026年5月23日`。
- 点击“认识时间”卡片后，切换显示“现实相识时间”。
- 现实相识时间可以在资料设置页里手动设置。
- 如果没有设置现实相识时间，显示“未设置现实相识时间”。

后端需要保存：

- 好友关系创建/接受时间：可用现有 `Friend.createdAt`，但最好确认是否表示接受时间。
- 现实相识时间：建议字段 `Friend.realMetAt`。

## 5. 回声值

“亲密度”这个名字不要用。

推荐命名：

- 页面展示：`回声值`
- 排行页：`回声排行`
- 说明文案：`由聊天、动态互动和通话等行为累积`

初始实现可以先做 MVP：

- 每发送一条消息 +1
- 每点赞对方动态 +2
- 每评论对方动态 +3
- 每次语音/通话完成 +5
- 每天最多计入一定上限，避免刷分

需要新增：

- 后端统计服务，例如 `relationshipScore.service.ts`
- 前端好友回声排行页
- 关系星域页展示当前好友的回声值

## 6. 最近互动 / 最近连接

“最近互动”现在也是暂无，含义不够明确。

建议改成：

- 名称：`最近连接`
- 内容优先级：
  1. 最近聊天时间
  2. 最近动态互动时间
  3. 最近通话时间
  4. 没有则显示“暂无”

## 7. 侧边栏“星域”点击问题

当前点击侧边栏“星域”后仍然像刷新一样回到会话列表，这是不对的。

预期：

- 点击侧边栏“星域”进入星域管理页 `/star-zones`。
- 不应该回到 `/` 会话列表。
- 不应该只是关闭侧边栏。

需要检查：

- `SidebarDrawer.tsx` 里星域按钮的 `onClick` 是否实际执行 `navigate('/star-zones')`。
- `App.tsx` 是否正确注册 `/star-zones`。
- `StarZoneManagePage` 是否运行时异常，异常可能导致路由回退。
- 是否有移动端手势或关闭侧边栏逻辑覆盖了 navigate。
- 是否线上缓存仍加载旧 JS。

## 8. 好友资料入口统一

最终入口设计：

- 聊天页顶部点击好友名字：不进入设置。
- 聊天页左滑：进入“与 XXX 的星域”。
- 关系星域页点击“资料设置”：进入好友设置页。
- 好友设置页返回：回到关系星域页。

这样聊天页不塞太多入口，结构更清楚。

## 9. 下一轮执行顺序

1. 修复侧边栏“星域”跳转问题。
2. 修复“资料设置”按钮跳转，并关闭聊天页标题点击入口。
3. 调整关系星域四个数据块文案和布局。
4. 实现展示星域选择弹窗。
5. 实现认识时间展示和现实相识时间设置入口。
6. 设计并预留回声值数据结构。
7. 增加好友回声排行页。
8. 隐藏普通用户设置页的服务器地址，仅开发模式显示。

## 10. 不做的事

- 不删除数据。
- 不改 `.env`。
- 不动 uploads。
- 不动 Docker volume。
- 不执行数据库清空或重置。

## 11. 小修进度记录

执行时间：2026-05-23

已实现，等待后续检验：

- 侧边栏“星域”点击逻辑已调整为先关闭侧边栏，再进入 `/star-zones`，用于避免移动端遮罩/手势状态影响跳转。
- “资料设置”按钮已改为进入好友设置页：`/chat/:digitalId/settings?from=orbit`。
- 聊天页顶部点击好友名字进入设置的入口已关闭。
- 好友设置页从关系星域进入时，返回会回到 `/chat/:digitalId?orbit=1`，并自动重新打开“与 XXX 的星域”。
- 关系星域四个数据块文案已先改为：展示星域、认识时间、回声值、最近连接。

仍待后续完整实现：

- 展示星域选择弹窗和后端保存字段。
- 认识时间真实数据和现实相识时间设置。
- 回声值统计、排行页和防刷规则。
- 最近连接真实数据。
- 普通用户隐藏“服务器地址”。

## 12. 完整实现进度记录

执行时间：2026-05-24

已实现，等待后续检验：

- `Friend` 关系新增 `displayGroupId`、`realMetAt` 字段，用于保存展示星域和现实相识时间。
- 新增关系摘要接口：`GET /api/friends/relationship/:peerId`。
- 新增关系设置接口：`PATCH /api/friends/relationship/:peerId`。
- 新增回声排行接口：`GET /api/friends/echo-rankings`。
- 关系星域页现在读取真实关系摘要：
  - 展示星域来自好友所属星域，可点击自定义弹窗选择。
  - 认识时间默认显示 Echo 加好友时间，点击后切换现实相识时间。
  - 回声值由历史聊天、动态点赞、动态评论累计计算。
  - 最近连接取最近聊天、点赞、评论时间。
- 新增“回声排行”页面：`/echo-rankings`，展示自己和所有好友的回声值排行。
- 好友设置页新增“现实相识时间”输入和保存。
- 普通用户设置页隐藏“服务器地址”；开发者账号（按实际配置）或本地 `echo-dev-mode=true` 才显示。

后续可继续优化：

- 回声值目前是轻量实时统计，没有单独流水表和每日防刷上限；后续如果用户量上来，可以改为增量积分表。
- 语音/通话完成计分还未接入，因为当前通话成功/结束事件还需要单独确认。
- 现实相识时间目前在好友设置页里设置，后续可以做成更漂亮的底部弹窗。

## 13. 2026-05-24 聊天稳定性修复记录

已实现并部署，等待手机端继续检验：

- 修复自己发送消息本地出现两条：后端不再把发送者自己的消息再广播给发送端，发送端只通过 ack 替换本地临时消息。
- 修复未读数有时退出会话后仍存在：打开会话时不再受“已读回执开关”影响，始终会为自己清未读；已读回执通知仍受开关控制。
- 修复聊天背景互相同步：好友关系新增 `chatBackgroundUser`、`chatBackgroundFriend`，聊天专属背景按设置者单独保存。
- 修复通知在当前聊天内仍弹出：前端记录当前打开的聊天 peerId，通知层会屏蔽当前会话消息。
- 修复通知点击跳转目标错误：通知 chatId 改为发送者 id。
- 修复会话列表长按“删除会话”无效果：现在会调用 `/api/messages/clear` 并从本地列表移除。
- 修复在线状态初始不同步：连接 socket 后后端下发当前在线列表，前端不再只等待后续 online update。
- 改善键盘弹出遮挡最新消息：监听 `visualViewport.resize`，输入框聚焦后滚动到最新消息。
- 改善定时发送面板：时间、内容、确定/取消改为两列布局，避免确定按钮被挤没。

仍需继续观察或单独深入：

- 语音录音和播放在不同手机浏览器、5+ App、HTTP/HTTPS 下行为不同；本轮未完全重做语音链路。
- 动态发布页已有图片上传、展示模式、星域权限入口；如果线上仍看不到，优先检查手机是否加载旧包或页面是否被样式遮挡。
