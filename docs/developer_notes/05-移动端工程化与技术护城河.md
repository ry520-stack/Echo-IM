# Echo 稀土掘金篇 5：移动端工程化 — 性能与体验的护城河

## 防内存溢出 (OOM) 管线

手机原图可达 8-12MB，直接上传会导致浏览器内存溢出和请求超时。

### 前端压缩方案
```typescript
import imageCompression from 'browser-image-compression';

export async function compressImage(file: File): Promise<File> {
  return await imageCompression(file, {
    maxSizeMB: 0.3,           // 最大 300KB
    maxWidthOrHeight: 1600,   // 限制分辨率
    useWebWorker: true,       // 不阻塞主线程
    fileType: 'image/webp',   // WebP 格式
    initialQuality: 0.7,      // 初始质量
  });
}
```

效果：8MB → 200KB，上传速度提升 40 倍。

## 移动端键盘避让方案

传统 `h-screen` 在键盘弹出时布局崩溃，出现半屏黑屏。

### 解决方案
```html
<div className="min-h-[100dvh] flex flex-col overflow-y-auto">
```

- `dvh` (dynamic viewport height) 随键盘自动调整
- `overflow-y-auto` 内部滚动
- `pb-[env(safe-area-inset-bottom)]` 安全区适配

## 乐观更新 (Optimistic UI)

在弱网或 VPN 环境下，发送消息立即本地渲染：

```typescript
// 1. 立即创建临时消息
const tempId = 'temp-' + Date.now();
const optimisticMsg = { ... };
setMessages(prev => [...prev, optimisticMsg]);

// 2. 异步发送
socket.emit('message:send', ..., (res) => {
  if (res?.error) {
    // 3. 失败标记红点
    setMessages(prev => prev.map(m => m.id === tempId ? {...m, content: m.content + ' ❌'} : m));
  } else {
    // 4. 替换为真实消息
    setMessages(prev => prev.map(m => m.id === tempId ? res.message : m));
  }
});
```

## 事件冒泡锁死机制

### 多重手势冲突解决方案

```typescript
// 侧边栏：仅左边缘 + 右滑
ChatPage touchEnd: edgeX < width*0.20 && dx > 40 → open sidebar

// 引力圈：全屏左滑
ConversationList touchEnd: dx < -50 → e.stopPropagation() + onOpenGravity()

// 聊天室左右滑
ChatWindow touchEnd: dx > 50 → onBack() | dx < -50 → showPersonalOrbit

// 长按菜单：300ms 锁
lockRef → 300ms 内拦截所有滑动事件
```

## 物理级阻尼动画

全 App 贯穿 Framer Motion 弹簧物理模型：
- 页面切换: `stiffness:150, damping:20, mass:0.8`
- 输入栏折叠: `stiffness:200, damping:25`
- 图片画廊: `stiffness:300, damping:25`
- Gooey 滑块: `stiffness:80-300, damping:15-30`

## 总结

Echo 的工程化实践围绕一个核心原则：**用户体验不能因性能问题而妥协**。前端压缩、乐观更新、手势锁死、物理动效——这四条技术护城河，让 Echo 在移动端的体验达到了原生应用的流畅度。

---

> 全系列完。代码仓库: https://github.com/ry520-stack/Echo-IM
